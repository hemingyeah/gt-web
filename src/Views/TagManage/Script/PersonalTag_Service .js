app.factory('PersonalTagService', function($http, service, DataOperate) {
	var ret = {
		// GetList: function($scope,data, fun) {
		// 	DataOperate.Save(Route.AccountLineBlockTagPermissions_GetList, data, function(data) {
		// 		if (fun) fun(data);
		// 	})
		// },
	};
	return ret;
});